---

---
<section id="topmainsection">
    <section class="tms_left">
        <h1>Jadilah Kader Terbaik</h1>
        <p>Tuntaskan kaderisasi, <br> dan jadilah bintang di masyarakat.</p>
        <div class="tms_cta">
            <a class="cta" href="/sop">Proses Seleksi</a>
            <a class="cta" href="/daftar" target="_blank">Daftar</a>
        </div>
    </section>
    <section class="tms_image">
        <img src="/images/ill01.png?nf_resize=fit&w=600&h=417" alt="" loading="lazy" decoding="async">
    </section>
</section>

<style lang="scss">
@use '../../styles/utils.scss';

#topmainsection {
    @apply grid grid-cols-1 md:grid-cols-2;
    section {
        @apply p-4 lg:p-8;
    }
    min-height: 65vh;
}

.tms_left {
    @apply flex flex-col justify-center order-2;
    h1 {
        @apply font-extrabold text-2xl md:text-4xl lg:text-5xl;
    }
    p {
        @apply mt-4 text-xl md:text-2xl lg:text-3xl;
    }
} 

.tms_image {
    @apply flex flex-col justify-center order-1;
    img {
        @apply object-contain;
    }
}

.tms_cta {
    @include utils.btn_box;
    @apply mt-8;
}
</style>